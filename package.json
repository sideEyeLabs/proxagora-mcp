{
  "name": "proxagora-mcp",
  "version": "0.1.0",
  "description": "MCP server for Proxagora — discover and call AI-native APIs from Claude",
  "main": "dist/index.js",
  "bin": { "proxagora-mcp": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "claude", "anthropic", "api-marketplace", "ai-agents", "proxagora"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0"
  }
}
